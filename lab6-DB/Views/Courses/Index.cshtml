@model IEnumerable<lab6_DB.Models.Course>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/myLayout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CrsName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CrsName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.CrsId }) |
            @Html.ActionLink("Details", "Details", new { id = item.CrsId }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.CrsId }) |
            @Html.ActionLink("Add Dept", "addDept", new { id = item.CrsId }) |
            @Html.ActionLink("Remove Dept", "removeDept", new { id = item.CrsId })
            


        </td>
    </tr>
}

</table>
